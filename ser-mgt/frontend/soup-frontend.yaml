# soup-frontend-deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: soup-frontend
  labels:
    type: frontend
  namespace: ser-mgt
spec:
  revisionHistoryLimit: 2
  replicas: 3
  selector:
    matchLabels:
      type: frontend
  template:
    metadata:
      labels:
        type: frontend
      annotations:
        container.apparmor.security.beta.kubernetes.io/soup-frontend: unconfined
    spec:
      containers:
         image: 664966433367.dkr.ecr.ap-northeast-2.amazonaws.com/soup-frontend:fd3ad42f9f78cf73a44c3ff681e5240111c3da72
          name: soup-frontend
          env:
            - name: MEMBER_URI
              value: soup-member-backend:8002
            - name: ADMIN_URI
              value: soup-admin-backend:8001
            - name: SEARCH_URI
              value: soup-search-backend:8000
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: tz-seoul
              mountPath: /etc/localtime
      volumes:
        - name: tz-seoul
          hostPath:
            path: /usr/share/zoneinfo/Asia/Seoul
